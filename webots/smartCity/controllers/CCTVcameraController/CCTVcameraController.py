import os


os.system('cmd /c "conda activate pyTorch && python CCTVcameraCode.py"')  # we run CCTVcameraCode.py code in another
# terminal after the "conda activate pyTorch" command so that we can exploit the libraries installed in the
# pyTorch conda environment
